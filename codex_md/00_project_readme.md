# RAFE NLP Project Readme

## 这个项目到底要干嘛

这个仓库的目标，是把一套面向金融文档的两阶段 NLP 流程搭起来，并逐步收口成稳定可复现的训练与评测 pipeline。

核心任务链路是：

1. 从原始 filing 文本和已有标注结果中构建训练数据。
2. 训练一个能执行 Miner 和 Auditor 两类任务的 SFT / QLoRA 模型。
3. 将训练得到的 LoRA adapter merge 回基座模型。
4. 用 merged 模型跑 batch inference / eval，输出可用于后续分析的结果文件。

## 当前任务定义

当前代码里的任务拆分是：

- Miner:
  从原始文本中根据 schema 抽取与问题相关的原句和置信度。
- Auditor:
  基于当前证据和历史上下文，对单个 factor 给出情绪判断和置信度。

这两个任务都使用 chat `messages` 格式作为统一数据结构。

## 当前阶段的判断

这个仓库现在的重点，不是重新发明任务定义，而是把已经存在的主链路收口。

当前判断：

- 框架已经搭起来了，不是空壳。
- 已经存在 `build_dataset.py -> train.py -> qlora_merge.py -> qbatch.py` 这条主链路。
- 现在的主要问题不是“有没有功能”，而是“接口是否统一、路径是否统一、脚本是否能稳定端到端复现”。

## 未来 session 进入仓库时应先确认的事

每次新 session 接手时，优先确认：

1. 训练数据入口到底是本地 `save_to_disk` 数据集，还是 Hugging Face dataset name。
2. 训练输出目录、merge 输出目录、eval 模型目录是否一致。
3. 当前改动是在修“框架缺失”，还是在做“接口收口 / 参数统一 / 路径统一”。

## 判断标准

后续改动默认应服务于下面这个目标：

- 让这套流程从“能跑的实验脚本组合”变成“可复用、可解释、可重复执行”的项目框架。
